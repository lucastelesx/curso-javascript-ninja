(function(DOM, APP) {
  'use strict';
  function app(){
    const
      $imgUrl = new DOM('#image'),
      $brandModel = new DOM('#brand-model'),
      $year = new DOM('#year'),
      $plate = new DOM('#plate'),
      $color = new DOM('#color')
    
    function handleFormSubmit(event){
      event.preventDefault()
    }
  }
  app()
})(window.DOM, window.APP);
